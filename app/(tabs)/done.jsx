import { View, Text } from "react-native";
import DoneComponent from "../../components/done/DoneComponent";
import { StatusBar } from "expo-status-bar";

export default function DoneTasks(params) {
  return (
      <DoneComponent/>
  )
  
}